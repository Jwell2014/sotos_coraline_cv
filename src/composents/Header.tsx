import React from 'react';

const Header: React.FC = () => {
    return (
        <header className="text-white text-center">
            <h1 className="text-xl font-bold">Développeur full stack</h1>
        </header>
    );
};

export default Header;